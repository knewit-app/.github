# Contributing to knewit

knewit org의 모든 레포에 적용되는 공통 기여 가이드입니다.
레포별로 다른 규칙이 필요하면 **해당 레포의 `CONTRIBUTING.md`가 이 문서를 override** 합니다.

---

## 1. 시작하기 전에

- 작업 전에 이슈를 먼저 만들고, 자신을 assign 합니다. (사소한 오타 수정은 생략 가능)
- 이미 열려 있는 이슈·PR과 중복되지 않는지 확인합니다.
- 레포별 로컬 세팅 방법은 각 레포의 `README.md`를 참고하세요.

## 2. 브랜치 전략

기본은 **trunk 기반**입니다. `main`이 항상 배포 가능한 상태를 유지합니다.

```
main            # 배포 기준 브랜치, 직접 push 금지
└─ feat/#12-login-flow
└─ fix/#34-token-refresh
```

브랜치 이름: `<type>/#<이슈번호>-<짧은-설명>`

| type | 용도 |
| --- | --- |
| `feat` | 새 기능 |
| `fix` | 버그 수정 |
| `refactor` | 동작 변화 없는 구조 개선 |
| `chore` | 빌드, 설정, 의존성 |
| `docs` | 문서 |
| `test` | 테스트 코드 |

> release/develop 브랜치가 필요한 레포는 자체 가이드에 정의해 사용합니다.

## 3. 커밋 컨벤션

[Conventional Commits](https://www.conventionalcommits.org/)를 따릅니다.

```
<type>(<scope>): <제목>

<본문 — 무엇을 왜 바꿨는지. 선택>

Refs: #12
```

- type은 브랜치 type과 동일한 목록을 사용합니다.
- scope는 선택입니다. 예: `feat(auth): 소셜 로그인 추가`
- 제목은 50자 내외, 명령형 또는 간결한 서술형으로 작성합니다.
- 커밋은 되도록 하나의 논리적 변경 단위로 유지합니다.

## 4. Pull Request

- PR 제목도 커밋 컨벤션을 따릅니다. (`feat(app): 홈 피드 무한 스크롤`)
- 템플릿의 항목을 채웁니다. 해당 없는 섹션은 삭제해도 됩니다.
- **Draft PR**을 적극 활용하세요. 방향만 먼저 확인받고 싶을 때 유용합니다.
- 리뷰어 **1명 이상 승인** 후 머지합니다.
- 머지 전략은 **Squash and merge**를 기본으로 합니다.
- 머지 후 브랜치는 삭제합니다.
- PR이 너무 커지면(대략 400줄 이상 변경) 나눠주세요. 리뷰 품질이 크게 떨어집니다.

### 리뷰 원칙

- 리뷰 코멘트는 사람이 아니라 코드를 향합니다.
- 반드시 고쳐야 하는 것과 제안을 구분해서 남깁니다. (`[must]`, `[nit]` 등)
- 답변 없이 24시간이 지나면 편하게 리마인드해주세요.

## 5. 코드 스타일

포매터·린터 설정은 각 레포에 포함되어 있습니다. **로컬에서 통과시킨 뒤 push** 하세요.

| 영역 | 도구 |
| --- | --- |
| React Native / TypeScript | ESLint + Prettier |
| Spring (Kotlin/Java) | ktlint / google-java-format |
| FastAPI (Python) | Ruff (lint + format) |

- CI가 린트·테스트를 검사합니다. 실패한 PR은 머지하지 않습니다.
- 자동 포매팅으로 생긴 대규모 diff는 기능 변경과 분리된 커밋으로 남겨주세요.

## 6. 테스트

- 버그 수정 PR에는 가능하면 회귀 테스트를 함께 추가합니다.
- 새 기능은 핵심 로직 위주로 테스트를 작성합니다. 커버리지 수치보다 의미 있는 케이스를 우선합니다.
- 테스트가 어려운 변경이면 PR의 "확인 방법"에 수동 검증 절차를 적어주세요.

## 7. 보안 / 민감 정보

- API 키, 토큰, 인증서, 개인정보를 **절대 커밋하지 않습니다.**
- 환경 변수는 `.env.example`에 키 이름만 추가하고 값은 비워둡니다.
- 실수로 커밋했다면 즉시 해당 키를 폐기·재발급하고 메인테이너에게 알려주세요.
- 보안 취약점은 공개 이슈 대신 비공개 채널로 제보해주세요.

## 8. 이슈

- 버그는 `🐛 Bug Report`, 기능 제안은 `✨ Feature Request`, 그 외 작업(리팩터링·설정·문서·조사)은 `🛠 Task` 템플릿을 사용합니다.
- 질문·논의는 이슈보다 Discussions가 적합합니다.
- 라벨은 **종류**(`bug`, `enhancement`, `task`, `docs`)와 **영역**(`fe`, `be`, `infra`)을 조합해 사용합니다.
- 라벨 세트는 org 공통입니다. 새 레포를 만들면 [`scripts/sync-labels.sh`](https://github.com/knewit-app/.github/blob/main/scripts/sync-labels.sh)로 동기화하세요.
